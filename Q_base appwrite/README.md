# Q_base Appwrite Setup

Install required Python packages and run the database initialization script.

Install dependencies:

```bash
python3 -m pip install -r requirements.txt
```

Run the initializer (edit `app_write.py` to set `PROJECT_ID` and `API_KEY` first):

```bash
python3 app_write.py
```
