# Shared collections implemetation
       - diffrent indicator for shared collections for shared collections in lists
       -
       - one parameters to be added to make collection Shared with which group or separate table for shared collections
       - Parameter to admin only editing acess
       - and related features
       - report to gruop feature that puts a message on group
    Admins only change acess
        - non sharable
        - questions not deletable

# remove overflow from Top app bar question collection browser

#In sessions browser Session Title should be in Place holder in top app bar bring Session results icon to same row as masternavigator icon

#In notification message bubble show the real message instead of showing "ENCRYPTED MESSAGE" e2ee needed in transit only

#database error question or options id may be not unique every where

#Profile Screen
    - Back Nav leads to previous screen not home

##Home
    #Collection Creation
        - Change "Universal Creation" -> Create new collection
        - Multiple collection categories

    #Session Creation
        - Directly Navigate to session creation Wizard
            -fix back-nav

##Explore Screen
    - fix Shared indicator

    # collection overview
        - Separate logics implement for
            1. Shared
            2. Personal
    #Collection Browser
        - remove overflow from Top app bar question collection browser

##Connect Screen
    #P2P chat
    #Contact info
    #Group chat
        -fix top bar participant count
        #Shared Collections
        #Shared Sessions
        -collection or Session updates
        -mass update handling via appwrite


    #Group info
        -include at least to navigate to Shared collections or Sessions
        - Back nav to prev screen
        - Include full participant list
        - indicator for admins
        - Give admins acccess to make others admins

    #Block List
    #New P2P chat creation
    #New Group chat
        #Fix Navigate to created group after clicking create group


    #Sessions Screen
        Padding properly








